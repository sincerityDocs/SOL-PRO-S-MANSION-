{
  "name": "sol-pros-mansion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A lightweight shared front door for Sol Pro projects, state, and bounded agent handoffs.",
  "scripts": {
    "test": "node --test tests/mansion.test.mjs",
    "validate": "node scripts/validate-mansion.mjs",
    "resolve": "node scripts/resolve-project.mjs",
    "record": "node scripts/record-run.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
